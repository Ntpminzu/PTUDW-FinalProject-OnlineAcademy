{{#section "styles" append=true}}
<link rel="stylesheet" href="/category.css">
{{/section}}

<div class="edit-page">
  <div class="card edit-card">
    <h5 class="card-header">
      <i class="bi bi-pencil-square"></i> Edit Category
    </h5>

    <div class="card-body">
      <form action="/category/edit" method="POST">
        <div class="mb-3">
          <label for="catid" class="form-label">#</label>
          <input type="text" id="catid_display" class="form-control" value="{{category.CatID}}" disabled>
          <input type="hidden" name="catid" value="{{category.CatID}}">
        </div>
      
        <div class="mb-3">
          <label for="catName" class="form-label">Category Name</label>
          <input type="text" id="catName" name="catName" class="form-control" value="{{category.CatName}}" autofocus>
        </div>

        <div class="mb-3">
          <label for="catDesc" class="form-label">Description</label>
          <input type="text" id="catDesc" name="catDesc" class="form-control" value="{{category.CatDes}}" autofocus>
        </div>

        <div class="d-flex justify-content-end gap-2">
          <a href="/category/list" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Cancel
          </a>
          <button type="submit" class="btn btn-primary" formaction="/category/delete">
            <i class="bi bi-save"></i> Delete
          </button>
          <button type="submit" class="btn btn-primary" formaction="/category/patch">
            <i class="bi bi-save"></i> Save Changes
          </button>
        </div>
      </form>
    </div>

    <div class="card-footer text-muted text-center">
      Hogwarts Academy © 2025
    </div>
  </div>
